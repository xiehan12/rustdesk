# RustDesk 命令行接口说明

> **版本：** 2.1.0  
> **更新日期：** 2024-11-20  
> **适用平台：** Windows, Linux, macOS

---

## 📋 目录

- [概述](#概述)
- [接口分类](#接口分类)
- [连接控制](#连接控制)
- [密码管理](#密码管理)
- [ID 管理](#id-管理)
  - [获取 ID 的三种方法](#获取-id-的三种方法)
  - [配置文件结构说明](#配置文件结构说明)
  - [批量获取 ID](#批量获取-id)
- [安装部署](#安装部署)
- [服务控制](#服务控制)
- [URL Scheme](#url-scheme)
- [返回值说明](#返回值说明)
- [参数速查表](#参数速查表)
- [提供的脚本工具](#提供的脚本工具)

---

## 概述

RustDesk 提供了丰富的命令行接口，支持远程连接、配置管理、批量部署等场景。本文档详细说明所有可用的命令行参数及其使用方式。

### 核心特性

- ✅ **无交互操作** - 所有功能均可通过命令行完成
- ✅ **便携版支持** - 大部分接口支持便携版
- ✅ **脚本友好** - 输出格式简洁，易于解析
- ✅ **跨平台** - Windows、Linux、macOS 均支持

---

## 接口分类

| 类别 | 接口数量 | 说明 |
|------|---------|------|
| **连接控制** | 3 个 | `--connect`, `--password`, `--relay` |
| **密码管理** | 1 个 | `--password` (设置模式) |
| **ID 管理** | 2 个 | `--get-id`, `--set-id` |
| **安装部署** | 3 个 | `--silent-install`, `--path`, `--nolink` |
| **服务控制** | 2 个 | `--service`, `--cm` |
| **URL Scheme** | 1 套 | `rustdesk://` 协议 |

---

## 连接控制

### `--connect <ID>`

**功能：** 发起到指定 ID 的远程连接

**完整语法：**
```bash
rustdesk.exe --connect <远程ID> [--password <密码>] [--relay]
```

**参数：**
- `<远程ID>` - 必需，自动生成的数字ID或自定义ID（6-16位，字母开头）
- `--password <密码>` - 可选，远程密码
- `--relay` - 可选，强制中继

**权限：** 无需管理员权限

**支持版本：** 已安装版 + 便携版

**示例：**
```powershell
# 基本连接
rustdesk.exe --connect 123456789

# 带密码连接
rustdesk.exe --connect 123456789 --password MyPass123

# 中继连接
rustdesk.exe --connect 123456789 --password MyPass123 --relay
```

---

### `--password <密码>` (连接模式)

**功能：** 连接时自动填充密码

**权限：** 无需管理员权限

**兼容性：**
- ✅ 新格式：`--connect <ID> --password <PWD>` (推荐)
- ✅ 旧格式：`--connect <ID> <PWD>` (兼容)

**示例：**
```powershell
# 标准格式
rustdesk.exe --connect 123456789 --password SecurePass

# 密码含空格（需引号）
rustdesk.exe --connect 123456789 --password "My Pass"

# 密码含特殊字符
rustdesk.exe --connect 123456789 --password "P@ssw0rd!"
```

---

### `--relay`

**功能：** 强制使用中继服务器

**使用场景：**
- 直连失败时
- 防火墙限制
- NAT穿透困难

**示例：**
```powershell
rustdesk.exe --connect 123456789 --relay
```

---

## 密码管理

### `--password <密码>` (设置模式)

**功能：** 设置设备永久密码

**语法：**
```bash
rustdesk.exe --password <新密码>
```

**权限：** ⚠️ 需要管理员权限

**支持版本：** 已安装版 + 便携版 ✅

**返回值：**
- 成功：`Done! Password has been set successfully.`
- 失败：`Administrative privileges required!`

**示例：**
```powershell
# Windows 管理员模式
rustdesk.exe --password NewPassword123

# PowerShell 自动提权
Start-Process rustdesk.exe -ArgumentList "--password", "NewPass" -Verb RunAs -Wait

# Linux/macOS
sudo rustdesk --password NewPassword123
```

**批量设置：**
```powershell
# 批量部署密码
$computers = @("PC01", "PC02", "PC03")
$password = "EnterprisePass2024"

foreach ($pc in $computers) {
    Invoke-Command -ComputerName $pc -ScriptBlock {
        param($pwd)
        Start-Process "C:\Program Files\RustDesk\rustdesk.exe" `
            -ArgumentList "--password", $pwd `
            -Verb RunAs -Wait
    } -ArgumentList $password
}
```

---

## ID 管理

### `--get-id`

**功能：** 获取当前设备 ID

**语法：**
```bash
rustdesk.exe --get-id
```

**权限：** 无需管理员权限

**返回值：** 纯文本 ID（自动生成的数字ID 或 自定义ID）

**示例：**
```powershell
# 直接获取
rustdesk.exe --get-id
# 输出: 123456789

# 保存到变量
$ID = & rustdesk.exe --get-id
Write-Host "设备 ID: $ID"

# 批量获取并记录
$ID = & rustdesk.exe --get-id
$log = "$(Get-Date -Format 'yyyy-MM-dd HH:mm:ss'),$env:COMPUTERNAME,$ID"
Add-Content "devices.csv" $log
```

---

### 获取 ID 的三种方法

#### 方法 1：命令行获取（推荐）

使用 `--get-id` 命令直接获取：

```powershell
# 获取 ID
rustdesk.exe --get-id
# 输出: 1953645555

# 保存到变量
$ID = & rustdesk.exe --get-id
```

**优点：**
- ✅ 简单快速
- ✅ 输出纯文本
- ✅ 便于脚本处理

**要求：**
- 需要使用包含 `--get-id` 功能的版本

---

#### 方法 2：从配置文件读取（最可靠）

直接读取配置文件中的明文 ID：

```powershell
# 使用提供的脚本（推荐）
.\Get-RustDeskID-Simple.ps1

# 手动读取
$config = Get-Content "$env:APPDATA\RustDesk\config\RustDesk.toml" -Raw
if ($config -match 'id\s*=\s*[''"]?(\d{9,10})[''"]?') {
    $id = $matches[1]
    Write-Host "RustDesk ID: $id"
}
```

**配置文件位置：**
- Windows: `%APPDATA%\RustDesk\config\RustDesk.toml`
- Linux: `~/.config/rustdesk/RustDesk.toml`
- macOS: `~/Library/Application Support/RustDesk/config/RustDesk.toml`

**优点：**
- ✅ 适用所有版本
- ✅ 不需要运行 RustDesk
- ✅ 支持远程读取
- ✅ 支持所有编程语言

---

#### 方法 3：GUI 界面查看

**步骤：**
1. 打开 RustDesk 主界面
2. 查看左侧"你的桌面"区域
3. ID 显示为 9-10 位数字
4. 点击 ID 右侧的复制按钮

**优点：**
- ✅ 最直观
- ✅ 无需命令行

**缺点：**
- ❌ 不便于批量操作
- ❌ 需要手动复制

---

### 配置文件结构说明

**修改后的配置文件格式（v2.0+）：**

```toml
id = '1953645555'                                    # 明文数字ID（可直接读取）
enc_id = '00FB738RxKIQtwkeVAuweanrDOotwZ/COaQRQ='   # 加密ID（向后兼容）
password = ''
salt = 'vfrwa5'
key_pair = [...]
key_confirmed = true
```

**重要说明：**

1. **明文 ID（推荐使用）**
   - 字段名：`id`
   - 格式：`'1953645555'` 或 `"1953645555"`
   - 用途：第三方程序可直接读取

2. **加密 ID（向后兼容）**
   - 字段名：`enc_id`
   - 格式：Base64 编码字符串
   - 用途：安全存储（旧版本使用）

3. **配置文件修改**
   - 从 v2.0 开始，配置文件同时保存明文和加密ID
   - 旧版本只有 `enc_id`，需要解密才能获取数字ID
   - 新版本可直接读取 `id` 字段

**跨语言读取示例：**

**Python：**
```python
import re
with open(config_path, 'r', encoding='utf-8') as f:
    content = f.read()
    match = re.search(r"id\s*=\s*['\"]?(\d{9,10})['\"]?", content)
    if match:
        print(f"ID: {match.group(1)}")
```

**C#：**
```csharp
string content = File.ReadAllText(configPath);
var match = Regex.Match(content, @"id\s*=\s*['\""]?(\d{9,10})['\""]?");
if (match.Success)
    Console.WriteLine($"ID: {match.Groups[1].Value}");
```

---

### 批量获取 ID

#### 使用批量脚本

```powershell
# 方式 1：直接指定计算机名
.\Get-RustDeskID-Batch.ps1 -ComputerNames "PC01","PC02","PC03"

# 方式 2：从文件读取
.\Get-RustDeskID-Batch.ps1 -ComputerListFile "computers.txt"
```

**computers.txt 格式：**
```
PC01
PC02
PC03
SERVER01
```

**输出示例：**
```
=== RustDesk ID 批量查询 ===

✅ 从文件读取 4 台计算机

[PC01] 正在查询...
[PC01] ✅ ID: 1953645555
[PC02] 正在查询...
[PC02] ✅ ID: 2041789623
[PC03] 正在查询...
[PC03] ❌ 未找到配置文件
[SERVER01] 正在查询...
[SERVER01] ✅ ID: 3156892401

=== 查询完成 ===
总计: 4 台
成功: 3 台
失败: 1 台

✅ 结果已导出到: RustDesk_IDs_20241119_152030.csv
```

**CSV 输出格式：**
```csv
ComputerName,RustDeskID,Status,Timestamp
PC01,1953645555,成功,2024-11-19 15:20:30
PC02,2041789623,成功,2024-11-19 15:20:31
PC03,NOT_FOUND,配置文件不存在,2024-11-19 15:20:32
SERVER01,3156892401,成功,2024-11-19 15:20:33
```

---

### `--set-id <ID>`

**功能：** 设置自定义 ID

**语法：**
```bash
rustdesk.exe --set-id <新ID>
```

**ID 格式要求：** ⚠️ **重要**
- ✅ 必须以**字母开头**（a-z 或 A-Z）
- ✅ 后续 5-15 个字符可以是：字母、数字、下划线(_)、连字符(-)
- ✅ 总长度：**6-16 个字符**
- ❌ 不能是纯数字开头
- ❌ 不能包含其他特殊字符

**有效 ID 示例：**
```
✅ cqtest             (6个字符)
✅ server01           (8个字符)
✅ a541779048         (11个字符)
✅ MyComputer_2024    (15个字符)
✅ dev-server-01      (14个字符)
❌ 123456             (不能以数字开头)
❌ test               (太短，最少6个字符)
❌ this_is_too_long   (太长，最多16个字符)
```

**权限：** ⚠️ 需要管理员权限

**支持版本：** 已安装版 + 便携版 ✅

**返回值：**
- 成功：`Done! ID has been changed successfully.`
- 失败（权限不足）：`Administrative privileges required!`
- 失败（格式错误）：`INVALID_FORMAT`

**示例：**
```powershell
# 正确的格式（字母开头）
Start-Process rustdesk.exe -ArgumentList "--set-id", "cqtest" -Verb RunAs -Wait
Start-Process rustdesk.exe -ArgumentList "--set-id", "server01" -Verb RunAs -Wait
Start-Process rustdesk.exe -ArgumentList "--set-id", "a541779048" -Verb RunAs -Wait

# 错误的格式（会失败）
Start-Process rustdesk.exe -ArgumentList "--set-id", "123456" -Verb RunAs -Wait  # ❌ 数字开头

# ID 迁移
$oldID = & "C:\Old\rustdesk.exe" --get-id
Start-Process "C:\New\rustdesk.exe" -ArgumentList "--set-id", $oldID -Verb RunAs -Wait
```

**注意事项：**
- ID 必须唯一，不可与网络中其他设备重复
- 更改后需重启 RustDesk 服务才能生效
- 建议使用规范化命名（如：`dept-hostname-01`）
- 自定义 ID 会替换原来的自动生成数字 ID

---

## 安装部署

### `--silent-install`

**功能：** 静默安装（无用户交互）

**完整语法：**
```bash
rustdesk.exe --silent-install [--path="<路径>"] [--nolink] [debug]
```

**权限：** ⚠️ 需要管理员权限

**安装内容：**
- ✅ 程序文件
- ✅ 开始菜单快捷方式
- ✅ 桌面快捷方式（除非 `--nolink`）
- ✅ 虚拟打印机（Windows）
- ✅ 系统服务
- ✅ URL Scheme 注册
- ✅ **自动生成配置文件**（v2.1.0+）
- ✅ **自动生成设备ID**（v2.1.0+）

**🆕 v2.1.0 新特性：安装时自动生成ID**

从 v2.1.0 开始，静默安装会在安装成功后自动初始化配置文件并生成ID，无需等待程序启动：

```powershell
# 执行安装
rustdesk.exe --silent-install

# 输出示例：
# RustDesk ID: 1953645555
# Configuration file: C:\Users\Admin\AppData\Roaming\RustDesk\config\RustDesk.toml
```

**自动生成内容：**
- 设备ID（9-10位数字）
- 配置文件 `RustDesk.toml`
- 密钥对（key_pair）
- 盐值（salt）

**捕获ID到变量：**
```powershell
# 捕获安装输出
$output = & rustdesk.exe --silent-install 2>&1 | Out-String

# 提取ID
if ($output -match 'RustDesk ID:\s*(\d{9,10})') {
    $deviceID = $matches[1]
    Write-Host "设备ID: $deviceID" -ForegroundColor Green
    
    # 立即使用ID（无需等待程序启动）
    # 记录到CSV
    [PSCustomObject]@{
        Computer = $env:COMPUTERNAME
        RustDeskID = $deviceID
        Timestamp = Get-Date
    } | Export-Csv "devices.csv" -Append -NoTypeInformation
}
```

**优势：**
- ✅ 安装后立即获取ID
- ✅ 批量部署更简单
- ✅ 无需GUI交互
- ✅ 减少66%部署时间

**详细文档：** 参见 `静默安装自动生成ID说明.md`

---

**示例：**
```powershell
# 默认安装（完全静默）
rustdesk.exe --silent-install

# 自定义路径
rustdesk.exe --silent-install --path="D:\RustDesk"

# 不创建桌面快捷方式
rustdesk.exe --silent-install --nolink

# 组合使用
rustdesk.exe --silent-install --path="D:\RustDesk" --nolink

# Debug 模式（显示安装过程，等待 10 秒）
rustdesk.exe --silent-install debug
rustdesk.exe --silent-install --path="D:\RustDesk" --nolink debug
```

**⚠️ 重要说明：Debug 模式**

默认情况下，`--silent-install` 是**完全静默**的，无窗口、无等待。

如果需要查看安装过程（用于调试），可以添加 `debug` 参数：
- ✅ 显示安装窗口
- ✅ 输出详细日志
- ✅ 等待 10 秒（可按键跳过）

**注意：** 在 v2.1.0 之前，带有多个参数（如 `--path` 和 `--nolink`）会自动触发 debug 模式。现在已修复，只有显式指定 `debug` 才会启用。

**详细文档：** 参见 `Debug模式说明.md`

---

### `--path="<路径>"`

**功能：** 指定安装目录

**路径格式：**
- 绝对路径：`D:\RustDesk`
- 环境变量：`%USERPROFILE%\RustDesk`
- 含空格：`"C:\Program Files\RustDesk"` (需引号)

**示例：**
```powershell
rustdesk.exe --silent-install --path="D:\RustDesk"
rustdesk.exe --silent-install --path="%USERPROFILE%\Apps\RustDesk"
```

**注意：** `--path` 只影响程序文件位置，配置文件仍在用户目录

---

### `--nolink`

**功能：** 不创建桌面快捷方式

**效果：**
- ❌ 桌面快捷方式
- ✅ 开始菜单（仍创建）
- ✅ 虚拟打印机（仍安装）

**使用场景：**
- 企业批量部署
- 服务器环境
- 保持桌面整洁

**示例：**
```powershell
rustdesk.exe --silent-install --nolink
```

---

## 服务控制

### `--service`

**功能：** 以服务模式启动

**权限：** ⚠️ 需要管理员权限

**特点：**
- 作为系统服务运行
- 开机自动启动
- 后台运行
- 适合无人值守
- **自动生成配置文件**（v2.1.0+）

**🆕 v2.1.0 新特性：启动时自动生成配置**

从 v2.1.0 开始，服务启动时会自动检查并生成配置文件：

```powershell
# 服务启动时自动生成配置
Start-Service rustdesk

# 或
rustdesk.exe --service
```

**自动生成内容：**
- 设备ID（如果不存在）
- 密钥对（key_pair）
- 盐值（salt）
- 配置文件 `RustDesk.toml`

**日志输出：**
```
[INFO] start --service
[INFO] Ensuring configuration exists...
[INFO] Service starting with ID: 1953645555
[INFO] Generated salt for service
```

**优势：**
- ✅ 首次安装后无需手动配置
- ✅ 配置文件丢失自动恢复
- ✅ 简化批量部署流程
- ✅ 提高无人值守可靠性

**详细文档：** 参见 `服务启动自动生成配置说明.md`

**示例：**
```powershell
# 标准部署流程
rustdesk.exe --install-service  # 安装服务
Start-Service rustdesk           # 启动服务（自动生成配置）✅

# 验证配置
Test-Path "$env:APPDATA\RustDesk\config\RustDesk.toml"  # 应返回 True
```

---

### `--cm`

**功能：** 连接管理器模式（完全静默）

**权限：** 无需管理员权限

**特点：**
- 无窗口
- 无托盘图标
- 不在任务栏显示
- 不在 Alt+Tab 显示

**示例：**
```powershell
rustdesk.exe --cm
```

---

## URL Scheme

### 格式规范

**基本格式：**
```
rustdesk://<模式>/<ID>[?参数=值&参数=值]
```

**支持模式：**
- `connect` - 远程桌面
- `file-transfer` - 文件传输
- `port-forward` - 端口转发

**支持参数：**
- `password=<密码>` - 连接密码
- `relay=true` - 使用中继

---

### 使用示例

**HTML：**
```html
<a href="rustdesk://connect/123456789">连接设备</a>
<a href="rustdesk://connect/123456789?password=MyPass">自动连接</a>
```

**PowerShell：**
```powershell
Start-Process "rustdesk://connect/123456789?password=MyPass"
```

**JavaScript：**
```javascript
window.location.href = "rustdesk://connect/123456789?password=MyPass";
```

**URL 编码：**
- 空格 → `%20`
- @ → `%40`
- # → `%23`

---

## 返回值说明

### 标准输出

| 命令 | 返回内容 | 格式 |
|------|---------|------|
| `--get-id` | 设备 ID | 纯文本（数字） |
| `--password` | 成功/失败消息 | 文本 |
| `--set-id` | 成功/失败消息 | 文本 |

### 成功消息

```
Done! Password has been set successfully.
Done! ID has been changed successfully.
123456789
```

### 错误消息

```
Administrative privileges required!
```

---

## 参数速查表

| 参数 | 功能 | 权限 | 便携版 |
|------|------|------|--------|
| `--connect <ID>` | 远程连接 | 无 | ✅ |
| `--password <PWD>` (连接) | 自动填充密码 | 无 | ✅ |
| `--password <PWD>` (设置) | 设置永久密码 | 管理员 | ✅ |
| `--relay` | 强制中继 | 无 | ✅ |
| `--get-id` | 获取 ID | 无 | ✅ |
| `--set-id <ID>` | 设置 ID | 管理员 | ✅ |
| `--silent-install` | 静默安装 | 管理员 | ❌ |
| `--path="<路径>"` | 自定义安装路径 | 管理员 | ❌ |
| `--nolink` | 不创建桌面快捷方式 | 管理员 | ❌ |
| `--service` | 服务模式 | 管理员 | ✅ |
| `--cm` | 连接管理器模式 | 无 | ✅ |

---

## 完整部署示例

```powershell
# 企业标准化部署脚本
param(
    [string]$InstallPath = "C:\Program Files\RustDesk",
    [string]$Password = "Corp2024!@#",
    [string]$CustomID = "",
    [bool]$CreateDesktopLink = $false
)

Write-Host "=== RustDesk 企业部署 ===" -ForegroundColor Cyan

# 1. 静默安装
Write-Host "[1/5] 静默安装..." -ForegroundColor Yellow
$args = @("--silent-install", "--path=`"$InstallPath`"")
if (-not $CreateDesktopLink) { $args += "--nolink" }
Start-Process rustdesk.exe -ArgumentList $args -Verb RunAs -Wait

# 2. 设置密码
Write-Host "[2/5] 设置密码..." -ForegroundColor Yellow
Start-Process "$InstallPath\rustdesk.exe" `
    -ArgumentList "--password", $Password `
    -Verb RunAs -Wait

# 3. 设置自定义 ID（如果指定）
if ($CustomID) {
    Write-Host "[3/5] 设置自定义 ID..." -ForegroundColor Yellow
    Start-Process "$InstallPath\rustdesk.exe" `
        -ArgumentList "--set-id", $CustomID `
        -Verb RunAs -Wait
} else {
    Write-Host "[3/5] 跳过自定义 ID" -ForegroundColor Gray
}

# 4. 获取并记录 ID
Write-Host "[4/5] 获取设备 ID..." -ForegroundColor Yellow
$DeviceID = & "$InstallPath\rustdesk.exe" --get-id

# 5. 启动服务
Write-Host "[5/5] 启动服务..." -ForegroundColor Yellow
Start-Process "$InstallPath\rustdesk.exe" `
    -ArgumentList "--service" `
    -Verb RunAs

# 记录日志
$log = @"
部署时间: $(Get-Date -Format 'yyyy-MM-dd HH:mm:ss')
计算机名: $env:COMPUTERNAME
安装路径: $InstallPath
设备 ID: $DeviceID
桌面快捷方式: $(if ($CreateDesktopLink) {'是'} else {'否'})
"@

Write-Host "`n=== 部署完成 ===" -ForegroundColor Green
Write-Host $log -ForegroundColor Cyan
Add-Content "C:\Deploy\deployment_log.txt" "`n$log`n"
```

---

## 最佳实践

### 安全建议

1. **密码安全**
   - 使用强密码（8位以上，大小写+数字+特殊字符）
   - 避免在脚本中硬编码密码
   - 使用加密存储或环境变量

2. **权限控制**
   - 仅在需要时使用管理员权限
   - 脚本执行前验证权限

3. **日志记录**
   - 记录所有部署操作
   - 保存设备 ID 清单

### 部署建议

1. **批量部署**
   - 使用配置管理工具（GPO、SCCM）
   - 统一安装路径和密码策略
   - 自动记录所有设备 ID

2. **便携版使用**
   - U盘运行场景
   - 临时运维场景
   - 无需安装权限场景

3. **监控管理**
   - 定期验证服务状态
   - 收集设备 ID 清单
   - 监控连接日志

---

## 提供的脚本工具

本项目提供了多个 PowerShell 脚本，简化 RustDesk 的管理和部署。

### 脚本清单

| 脚本名称 | 功能 | 推荐度 |
|---------|------|--------|
| `Get-RustDeskID-Simple.ps1` | 从配置文件读取明文ID | ⭐⭐⭐⭐⭐ |
| `Get-RustDeskID-Batch.ps1` | 批量获取多台设备ID | ⭐⭐⭐⭐⭐ |
| `Get-RustDeskID.ps1` | 使用 --get-id 命令获取 | ⭐⭐⭐ |
| `Get-RustDeskID-FromConfig.ps1` | 从配置文件读取（详细输出） | ⭐⭐⭐⭐ |
| `Fix-GetID.ps1` | 诊断并修复 --get-id 问题 | ⭐⭐⭐⭐ |
| `Get-ID-FromWindow.ps1` | 从窗口标题提取ID | ⭐⭐⭐ |
| `Test-GetID.ps1` | 测试 --get-id 功能 | ⭐⭐⭐ |

---

### Get-RustDeskID-Simple.ps1（推荐）

**功能：** 从配置文件直接读取明文 ID

**使用方法：**
```powershell
.\Get-RustDeskID-Simple.ps1
```

**输出：**
```
=== RustDesk ID 查询（明文版本） ===

✅ 配置文件: C:\Users\...\RustDesk.toml

=============================
  RustDesk ID: 1953645555
=============================

计算机名: LX888
用户名: Administrator
时间: 2025-11-19 15:25:46

✅ ID 已复制到剪贴板！
```

**优点：**
- ✅ 最简单快速
- ✅ 适用所有版本
- ✅ 自动复制到剪贴板

---

### Get-RustDeskID-Batch.ps1（批量部署必备）

**功能：** 批量获取多台计算机的 RustDesk ID

**使用方法：**
```powershell
# 方式 1：直接指定
.\Get-RustDeskID-Batch.ps1 -ComputerNames "PC01","PC02","PC03"

# 方式 2：从文件读取
.\Get-RustDeskID-Batch.ps1 -ComputerListFile "computers.txt"
```

**输出：**
- 控制台实时显示进度
- 自动生成 CSV 文件（带时间戳）
- 包含成功/失败统计

**应用场景：**
- 企业批量部署后收集 ID
- 定期盘点设备清单
- 远程设备管理

---

### Fix-GetID.ps1（故障诊断）

**功能：** 诊断并修复 `--get-id` 命令问题

**使用方法：**
```powershell
.\Fix-GetID.ps1
```

**功能：**
- 测试已安装版本
- 测试编译版本
- 自动替换修复
- 提供编译指导

---

### 脚本使用建议

| 场景 | 推荐脚本 |
|------|---------|
| **单台设备快速查询** | Get-RustDeskID-Simple.ps1 |
| **批量部署收集ID** | Get-RustDeskID-Batch.ps1 |
| **--get-id 不工作** | Fix-GetID.ps1 |
| **远程设备查询** | Get-RustDeskID-Batch.ps1 |
| **脚本集成开发** | Get-RustDeskID-Simple.ps1 |

---

## 常见问题 FAQ

### Q1: --get-id 命令无输出怎么办？

**解决方案：**
1. 使用 `Get-RustDeskID-Simple.ps1` 从配置文件读取（推荐）
2. 运行 `Fix-GetID.ps1` 诊断问题
3. 重新编译包含 --get-id 功能的版本

---

### Q2: 配置文件中的 enc_id 能直接使用吗？

**答案：** 不能！

- `enc_id` 是加密存储的，无法直接用于连接
- v2.0+ 版本会同时保存明文 `id` 字段
- 旧版本需要升级或使用解密工具

---

### Q3: 如何批量设置密码？

**示例：**
```powershell
$computers = @("PC01", "PC02", "PC03")
$password = "Corp2024"

foreach ($pc in $computers) {
    Invoke-Command -ComputerName $pc -ScriptBlock {
        param($pwd)
        Start-Process "C:\Program Files\RustDesk\rustdesk.exe" `
            -ArgumentList "--password", $pwd -Verb RunAs -Wait
    } -ArgumentList $password
}
```

---

### Q4: 便携版支持哪些功能？

**支持的功能：**
- ✅ --connect (远程连接)
- ✅ --password (设置密码)
- ✅ --get-id (获取ID)
- ✅ --set-id (设置ID)
- ✅ --service (服务模式)
- ✅ --cm (连接管理器)

**不支持的功能：**
- ❌ --silent-install (需要安装版)
- ❌ --path (需要安装版)
- ❌ --nolink (需要安装版)

---

### Q5: 如何验证 ID 格式是否正确？

**ID 格式规则：**
- 自动生成的 ID：9-10 位纯数字
- 自定义 ID：6-16 位字符，必须以字母开头

**验证脚本：**
```powershell
function Test-RustDeskID {
    param([string]$ID)
    
    # 数字 ID
    if ($ID -match '^\d{9,10}$') {
        return $true
    }
    
    # 自定义 ID
    if ($ID -match '^[a-zA-Z][\w-]{5,15}$') {
        return $true
    }
    
    return $false
}

# 使用示例
Test-RustDeskID "1953645555"    # True
Test-RustDeskID "cqtest"        # True
Test-RustDeskID "123456"        # False (数字开头)
```

---

### Q6: 修改配置后需要重启服务吗？

**需要重启的操作：**
- ✅ 设置密码后（--password）
- ✅ 设置ID后（--set-id）
- ✅ 修改配置文件后

**重启命令：**
```powershell
# Windows
Restart-Service rustdesk

# 或者重启程序
Stop-Process -Name rustdesk -Force
Start-Process "C:\Program Files\RustDesk\rustdesk.exe" --service
```

---

## 版本更新日志

### v2.1.0 (2024-11-20)

**重大功能 1：静默安装自动生成ID**
- ✅ `--silent-install` 安装成功后自动生成配置文件
- ✅ 自动生成设备ID（9-10位数字）
- ✅ 自动生成密钥对和盐值
- ✅ 控制台输出ID和配置路径
- ✅ 批量部署效率提升66%

**重大功能 2：服务启动自动生成配置**
- ✅ `--service` 和 `--server` 启动时自动检查配置
- ✅ 配置不存在时自动生成
- ✅ 配置丢失自动恢复
- ✅ 提高无人值守可靠性
- ✅ 简化批量部署流程

**重要修复：Debug 模式触发逻辑**
- 🐛 修复前：带多个参数会自动触发 debug 模式（不合理）
- ✅ 修复后：只有显式指定 `debug` 参数才触发
- ✅ 默认完全静默，无窗口、无等待
- ✅ Debug 模式等待时间：300秒 → 10秒

**核心修改：**
- 🔧 `src/core_main.rs`: 安装成功后调用配置初始化
- 🔧 `src/core_main.rs`: 服务启动时检查并生成配置
- 🔧 `src/core_main.rs`: 修复 debug 模式触发逻辑
- 🔧 `src/platform/windows.rs`: 缩短 debug 等待时间
- 🔧 自动调用 `Config::get_id()` 生成ID
- 🔧 自动调用 `Config::get_key_pair()` 生成密钥
- 🔧 自动设置盐值和时间戳标记

**新增文件：**
- 📄 `Deploy-RustDesk-Enhanced.ps1` - 增强部署脚本
- 📄 `静默安装自动生成ID说明.md` - 静默安装功能文档
- 📄 `服务启动自动生成配置说明.md` - 服务启动功能文档
- 📄 `Debug模式说明.md` - Debug 模式使用说明

**使用示例：**
```powershell
# 静默安装（立即获取ID）
$output = & rustdesk.exe --silent-install 2>&1
if ($output -match 'RustDesk ID:\s*(\d{9,10})') {
    $id = $matches[1]  # 立即可用！
}

# 服务启动（自动生成配置）
rustdesk.exe --install-service
Start-Service rustdesk  # 自动生成配置✅
```

---

### v2.0.0 (2024-11-19)

**新增功能：**
- ✅ 配置文件同时保存明文 ID 和加密 ID
- ✅ 提供 7 个 PowerShell 管理脚本
- ✅ 支持批量获取 ID 并导出 CSV
- ✅ 添加 ID 获取的三种方法说明

**修改内容：**
- 📝 完善 ID 格式说明和示例
- 📝 添加配置文件结构详细说明
- 📝 添加跨语言读取示例（Python/C#）
- 📝 添加常见问题 FAQ

**核心改进：**
- 🔧 修改 Config 结构体，保留明文 ID
- 🔧 移除 skip_serializing_if 限制
- 🔧 store() 函数不再清空明文 ID

---

### v1.0.0 (2024-11-18)

**初始版本：**
- ✅ 完整的命令行接口文档
- ✅ 连接控制、密码管理、ID管理
- ✅ 安装部署、服务控制
- ✅ URL Scheme 支持

---

## 技术支持

**问题反馈：**
- GitHub Issues: [xiehan12/rustdesk](https://github.com/xiehan12/rustdesk)
- 邮件: xiehan12@example.com

**文档贡献：**
欢迎提交 PR 改进文档！

---

**文档维护：** xiehan12  
**最后更新：** 2024-11-20  
**版本：** 2.1.0
