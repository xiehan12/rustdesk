# RustDesk 服务配置生成问题排查指南

## 🎯 问题描述

在使用 `rustdesk.exe --service` 启动服务时，配置文件可能未被自动创建。

---

## ✅ 解决方案（已实施）

### 增强的配置生成逻辑

**文件：** `src/core_main.rs`

**新增功能：**
1. ✅ **自动创建配置目录**
2. ✅ **详细日志输出**（记录配置文件路径）
3. ✅ **显式保存配置**（调用 `set_option` 强制写入）
4. ✅ **配置验证**（检查文件是否真的存在）

### 关键代码

```rust
// 1. 获取配置文件路径并记录
let config_file = Config::file();
log::info!("Config file path: {}", config_file.display());

// 2. 确保配置目录存在
if let Some(config_dir) = config_file.parent() {
    if !config_dir.exists() {
        log::info!("Creating config directory: {}", config_dir.display());
        std::fs::create_dir_all(config_dir)?;
    }
}

// 3. 生成 ID（会自动保存）
let id = Config::get_id();

// 4. 生成密钥对（会自动保存）
let _ = Config::get_key_pair();

// 5. 生成盐值
if Config::get_salt().is_empty() {
    Config::set_salt(&Config::get_auto_password(6));
}

// 6. 显式保存配置（确保写入）
Config::set_option("service_started_at".to_string(), get_time().to_string());

// 7. 验证配置文件是否存在
if config_file.exists() {
    log::info!("✅ Config file verified: exists");
} else {
    log::error!("❌ Config file not found after initialization!");
}
```

---

## 🔍 配置文件路径说明

### Windows 平台

**当前用户运行：**
```
C:\Users\<用户名>\AppData\Roaming\RustDesk\config\RustDesk.toml
```

**SYSTEM 账户运行（服务）：**
```
C:\Windows\System32\config\systemprofile\AppData\Roaming\RustDesk\config\RustDesk.toml
```

**LocalService 账户运行：**
```
C:\Windows\ServiceProfiles\LocalService\AppData\Roaming\RustDesk\config\RustDesk.toml
```

**NetworkService 账户运行：**
```
C:\Windows\ServiceProfiles\NetworkService\AppData\Roaming\RustDesk\config\RustDesk.toml
```

### 如何确定服务账户

```powershell
Get-WmiObject Win32_Service -Filter "Name='RustDesk'" | Select-Object StartName
```

---

## 🧪 测试步骤

### 方法 1：使用测试脚本（推荐）

```powershell
# 运行诊断脚本
.\Test-ServiceConfig.ps1

# 如果 RustDesk 不在当前目录
.\Test-ServiceConfig.ps1 -RustDeskPath "C:\Program Files\RustDesk\rustdesk.exe"
```

**脚本功能：**
- ✅ 检查 RustDesk 可执行文件
- ✅ 检查当前用户配置路径
- ✅ 检查 SYSTEM 账户配置路径
- ✅ 测试 ID 生成
- ✅ 检查服务状态
- ✅ 生成诊断报告

---

### 方法 2：手动测试

#### 步骤 1：停止现有服务

```powershell
Stop-Service RustDesk -ErrorAction SilentlyContinue
```

#### 步骤 2：清理旧配置（可选）

```powershell
# 当前用户配置
Remove-Item "$env:APPDATA\RustDesk\config\RustDesk.toml" -Force -ErrorAction SilentlyContinue

# SYSTEM 账户配置（需要管理员权限）
Remove-Item "C:\Windows\System32\config\systemprofile\AppData\Roaming\RustDesk\config\RustDesk.toml" -Force -ErrorAction SilentlyContinue
```

#### 步骤 3：重新编译并安装

```powershell
# 编译 Release 版本
cargo build --release

# 复制到安装目录
Copy-Item ".\target\release\rustdesk.exe" "C:\Program Files\RustDesk\rustdesk.exe" -Force

# 重新安装服务
& "C:\Program Files\RustDesk\rustdesk.exe" --silent-install
```

#### 步骤 4：启动服务

```powershell
Start-Service RustDesk
```

#### 步骤 5：检查日志

```powershell
# 查看应用程序日志
Get-EventLog -LogName Application -Source RustDesk -Newest 20 | Format-List

# 或查看日志文件（如果有）
Get-Content "C:\Windows\System32\config\systemprofile\AppData\Roaming\RustDesk\RustDesk.log" -Tail 50
```

#### 步骤 6：验证配置文件

```powershell
# 检查 SYSTEM 账户配置
$configPath = "C:\Windows\System32\config\systemprofile\AppData\Roaming\RustDesk\config\RustDesk.toml"

if (Test-Path $configPath) {
    Write-Host "✅ 配置文件存在" -ForegroundColor Green
    Get-Content $configPath
} else {
    Write-Host "❌ 配置文件不存在" -ForegroundColor Red
}
```

---

## 📊 日志输出示例

### 成功的日志输出

```
[INFO] start --service
[INFO] Ensuring configuration exists...
[INFO] Config file path: C:\Windows\System32\config\systemprofile\AppData\Roaming\RustDesk\config\RustDesk.toml
[INFO] Service starting with ID: 1953645555
[INFO] Key pair ensured
[INFO] Generated salt for service
[INFO] Configuration saved to: C:\Windows\System32\config\systemprofile\AppData\Roaming\RustDesk\config\RustDesk.toml
[INFO] ✅ Config file verified: exists
[INFO] Starting OS service...
```

### 失败的日志输出

```
[INFO] start --service
[INFO] Ensuring configuration exists...
[INFO] Config file path: C:\Windows\System32\config\systemprofile\AppData\Roaming\RustDesk\config\RustDesk.toml
[INFO] Creating config directory: C:\Windows\System32\config\systemprofile\AppData\Roaming\RustDesk\config
[ERROR] Failed to create config directory: Access denied
[WARN] Failed to get or generate ID
[ERROR] ❌ Config file not found after initialization!
```

---

## 🐛 常见问题排查

### 问题 1：配置目录无法创建

**症状：**
```
[ERROR] Failed to create config directory: Access denied
```

**原因：** 权限不足

**解决方案：**
```powershell
# 1. 检查服务账户
Get-WmiObject Win32_Service -Filter "Name='RustDesk'" | Select-Object StartName

# 2. 给 SYSTEM 账户授权（管理员运行）
$configDir = "C:\Windows\System32\config\systemprofile\AppData\Roaming\RustDesk\config"
New-Item -ItemType Directory -Path $configDir -Force
icacls $configDir /grant "NT AUTHORITY\SYSTEM:(OI)(CI)F" /T
```

---

### 问题 2：配置文件生成后立即消失

**症状：**
```
[INFO] ✅ Config file verified: exists
# 但之后文件消失了
```

**原因：** 可能被杀毒软件删除或其他进程干扰

**解决方案：**
```powershell
# 1. 添加杀毒软件排除项
# 排除路径：C:\Windows\System32\config\systemprofile\AppData\Roaming\RustDesk\

# 2. 检查文件系统事件
Get-WinEvent -LogName System -MaxEvents 100 | 
    Where-Object { $_.Message -like "*RustDesk*" }
```

---

### 问题 3：ID 无法生成

**症状：**
```
[WARN] Failed to get or generate ID
```

**原因：** 网络适配器问题或 MAC 地址获取失败

**解决方案：**
```powershell
# 1. 检查网络适配器
Get-NetAdapter | Where-Object { $_.Status -eq "Up" }

# 2. 手动设置 ID
rustdesk.exe --set-id "your_custom_id"

# 3. 查看详细错误日志
```

---

### 问题 4：配置生成但 ID 为空

**症状：** 配置文件存在，但 `id` 字段为空

**解决方案：**
```powershell
# 1. 删除配置文件
Remove-Item "$env:APPDATA\RustDesk\config\RustDesk.toml" -Force

# 2. 手动生成 ID
rustdesk.exe --get-id

# 3. 重启服务
Restart-Service RustDesk
```

---

## 🔧 手动创建配置（应急方案）

如果自动生成失败，可以手动创建配置文件：

```powershell
# 1. 创建配置目录
$configDir = "$env:APPDATA\RustDesk\config"
New-Item -ItemType Directory -Path $configDir -Force

# 2. 创建配置文件
$configContent = @"
enc_id = ""
id = "1234567890"
salt = "abcdef123456"
key_pair = (
    priv_key = "your_private_key",
    pub_key = "your_public_key"
)
"@

$configPath = "$configDir\RustDesk.toml"
$configContent | Out-File -FilePath $configPath -Encoding UTF8

Write-Host "✅ 配置文件已创建: $configPath"
```

**注意：** 手动创建的配置需要替换以下内容：
- `id`: 9-10 位数字
- `salt`: 随机字符串
- `key_pair`: 有效的密钥对

---

## 📝 完整部署脚本

```powershell
# RustDesk 完整部署脚本（含配置验证）

param(
    [string]$InstallerPath = "rustdesk.exe"
)

Write-Host "=== RustDesk 部署开始 ===" -ForegroundColor Cyan

# 1. 停止现有服务
Write-Host "1. 停止现有服务..." -ForegroundColor Yellow
Stop-Service RustDesk -ErrorAction SilentlyContinue

# 2. 静默安装
Write-Host "2. 执行静默安装..." -ForegroundColor Yellow
$output = & $InstallerPath --silent-install 2>&1 | Out-String
if ($output -match 'RustDesk ID:\s*(\d{9,10})') {
    $installID = $matches[1]
    Write-Host "   ✅ 安装成功，ID: $installID" -ForegroundColor Green
} else {
    Write-Host "   ⚠️  安装完成，但未获取到 ID" -ForegroundColor Yellow
}

# 3. 启动服务
Write-Host "3. 启动服务..." -ForegroundColor Yellow
Start-Service RustDesk
Start-Sleep -Seconds 3

# 4. 检查服务状态
Write-Host "4. 检查服务状态..." -ForegroundColor Yellow
$service = Get-Service RustDesk -ErrorAction SilentlyContinue
if ($service.Status -eq "Running") {
    Write-Host "   ✅ 服务运行中" -ForegroundColor Green
} else {
    Write-Host "   ❌ 服务未运行: $($service.Status)" -ForegroundColor Red
}

# 5. 验证配置文件
Write-Host "5. 验证配置文件..." -ForegroundColor Yellow
$configPaths = @(
    "$env:APPDATA\RustDesk\config\RustDesk.toml",
    "C:\Windows\System32\config\systemprofile\AppData\Roaming\RustDesk\config\RustDesk.toml"
)

$configFound = $false
foreach ($path in $configPaths) {
    if (Test-Path $path) {
        Write-Host "   ✅ 找到配置: $path" -ForegroundColor Green
        $configContent = Get-Content $path -Raw
        if ($configContent -match 'id\s*=\s*[''"]?(\d{9,10})[''"]?') {
            Write-Host "   🆔 服务 ID: $($matches[1])" -ForegroundColor Green
        }
        $configFound = $true
        break
    }
}

if (-not $configFound) {
    Write-Host "   ❌ 未找到配置文件" -ForegroundColor Red
    Write-Host "   建议检查日志：Get-EventLog -LogName Application -Source RustDesk -Newest 20" -ForegroundColor Yellow
}

# 6. 检查日志
Write-Host "6. 检查最近日志..." -ForegroundColor Yellow
$logs = Get-EventLog -LogName Application -Source RustDesk -Newest 5 -ErrorAction SilentlyContinue
if ($logs) {
    $logs | ForEach-Object {
        Write-Host "   [$($_.TimeGenerated)] $($_.Message.Substring(0, [Math]::Min(100, $_.Message.Length)))" -ForegroundColor Gray
    }
} else {
    Write-Host "   ⚠️  未找到日志" -ForegroundColor Yellow
}

Write-Host ""
Write-Host "=== 部署完成 ===" -ForegroundColor Cyan
```

---

## ✅ 验证清单

部署完成后，请检查以下项目：

- [ ] 服务状态为 "Running"
- [ ] 配置文件存在（SYSTEM 账户路径）
- [ ] 配置文件包含有效的 ID
- [ ] 配置文件包含密钥对
- [ ] 配置文件包含盐值
- [ ] 日志中有 "✅ Config file verified: exists"
- [ ] 无错误日志

---

## 📄 相关文档

| 文档 | 说明 |
|------|------|
| **Test-ServiceConfig.ps1** | 服务配置测试脚本 |
| **服务启动自动生成配置说明.md** | 功能详细说明 |
| **接口说明.md** | 完整接口文档 |

---

## 🆘 获取帮助

如果问题仍未解决，请提供以下信息：

1. **日志输出**
```powershell
Get-EventLog -LogName Application -Source RustDesk -Newest 20 | Format-List | Out-File rustdesk-logs.txt
```

2. **服务信息**
```powershell
Get-Service RustDesk | Format-List * | Out-File rustdesk-service.txt
Get-WmiObject Win32_Service -Filter "Name='RustDesk'" | Format-List * | Out-File rustdesk-service-details.txt
```

3. **配置文件检查**
```powershell
$paths = @(
    "$env:APPDATA\RustDesk\config\RustDesk.toml",
    "C:\Windows\System32\config\systemprofile\AppData\Roaming\RustDesk\config\RustDesk.toml"
)
foreach ($path in $paths) {
    "Path: $path" | Out-File rustdesk-config-check.txt -Append
    "Exists: $(Test-Path $path)" | Out-File rustdesk-config-check.txt -Append
    if (Test-Path $path) {
        Get-Content $path | Out-File rustdesk-config-check.txt -Append
    }
    "" | Out-File rustdesk-config-check.txt -Append
}
```

4. **诊断报告**
```powershell
.\Test-ServiceConfig.ps1 -RustDeskPath "C:\Program Files\RustDesk\rustdesk.exe"
```

---

**版本：** v2.1.0+  
**最后更新：** 2024-11-20  
**文档维护：** xiehan12
