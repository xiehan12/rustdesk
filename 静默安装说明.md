# RustDesk 静默安装说明

## 📋 静默安装参数

RustDesk 支持静默安装模式，无需用户交互即可完成安装。

### 主要安装参数

| 参数 | 说明 | 示例 |
|------|------|------|
| `--silent-install` | 静默安装主参数 | `rustdesk.exe --silent-install` |
| `--install` | 交互式安装（带UI） | `rustdesk.exe --install` |
| `--uninstall` | 卸载程序 | `rustdesk.exe --uninstall` |

---

## 🎯 静默安装选项

### Windows 平台默认选项

```rust
// 从 core_main.rs 第 303 行
let options = "desktopicon startmenu printer";
```

**包含的选项：**
- ✅ `desktopicon` - 创建桌面图标
- ✅ `startmenu` - 创建开始菜单快捷方式
- ✅ `printer` - 安装虚拟打印机（Windows独有）

### Linux/macOS 平台默认选项

```rust
// 从 core_main.rs 第 301 行
let options = "desktopicon startmenu";
```

**包含的选项：**
- ✅ `desktopicon` - 创建桌面图标
- ✅ `startmenu` - 创建开始菜单快捷方式

---

## 🚀 使用方法

### 1. 基本静默安装

```powershell
# 静默安装（使用默认选项）
rustdesk.exe --silent-install

# Windows: 会创建桌面图标 + 开始菜单 + 打印机
# Linux/macOS: 会创建桌面图标 + 开始菜单
```

### 2. 静默安装 + 调试模式

```powershell
# 静默安装并显示安装过程日志
rustdesk.exe --silent-install debug
```

### 3. 批量部署脚本

**PowerShell 脚本示例：**

```powershell
# silent_install.ps1
param(
    [string]$InstallerPath = "rustdesk.exe",
    [switch]$Debug
)

Write-Host "开始静默安装 RustDesk..." -ForegroundColor Green

if ($Debug) {
    & $InstallerPath --silent-install debug
} else {
    & $InstallerPath --silent-install
}

if ($LASTEXITCODE -eq 0) {
    Write-Host "安装成功！" -ForegroundColor Green
} else {
    Write-Host "安装失败，退出代码: $LASTEXITCODE" -ForegroundColor Red
}
```

**批处理脚本示例：**

```batch
@echo off
echo 开始静默安装 RustDesk...
rustdesk.exe --silent-install
if %ERRORLEVEL% EQU 0 (
    echo 安装成功！
) else (
    echo 安装失败！
)
pause
```

---

## 📂 默认安装位置

### Windows

| 项目 | 路径 |
|------|------|
| **程序目录** | `C:\Program Files\RustDesk\` |
| **可执行文件** | `C:\Program Files\RustDesk\rustdesk.exe` |
| **配置文件** | `C:\Windows\ServiceProfiles\LocalService\AppData\Roaming\RustDesk\` |
| **桌面快捷方式** | `%PUBLIC%\Desktop\RustDesk.lnk` |
| **开始菜单** | `%PROGRAMDATA%\Microsoft\Windows\Start Menu\Programs\RustDesk\` |

### Linux

| 项目 | 路径 |
|------|------|
| **程序目录** | `/usr/bin/` 或 `/opt/rustdesk/` |
| **配置文件** | `~/.config/rustdesk/` |
| **桌面快捷方式** | `~/Desktop/rustdesk.desktop` |

---

## 🔧 安装过程详解

### 静默安装流程

```
用户执行: rustdesk.exe --silent-install
    ↓
检查安装权限
    ↓
解析安装选项 (desktopicon, startmenu, printer)
    ↓
执行安装脚本
    ├─ 复制可执行文件到 Program Files
    ├─ 创建桌面快捷方式 (如果选择)
    ├─ 创建开始菜单快捷方式 (如果选择)
    ├─ 安装虚拟打印机 (Windows, 如果选择)
    ├─ 注册 Windows 服务
    └─ 写入注册表信息
    ↓
显示安装结果通知 (Toast)
    ↓
安装完成
```

### 核心代码位置

**文件：** `src/core_main.rs` 第 296-319 行

```rust
} else if args[0] == "--silent-install" {
    if config::is_disable_installation() {
        return None;
    }
    #[cfg(not(windows))]
    let options = "desktopicon startmenu";
    #[cfg(windows)]
    let options = "desktopicon startmenu printer";
    let res = platform::install_me(options, "".to_owned(), true, args.len() > 1);
    let text = match res {
        Ok(_) => translate("Installation Successful!".to_string()),
        Err(err) => {
            println!("Failed with error: {err}");
            translate("Installation failed!".to_string())
        }
    };
    Toast::new(Toast::POWERSHELL_APP_ID)
        .title(&config::APP_NAME.read().unwrap())
        .text1(&text)
        .sound(Some(Sound::Default))
        .duration(Duration::Short)
        .show()
        .ok();
    return None;
}
```

**文件：** `src/platform/windows.rs` 第 1351 行

```rust
pub fn install_me(options: &str, path: String, silent: bool, debug: bool) -> ResultType<()> {
    // 安装逻辑实现
    // - 检查 options 字符串中是否包含各种选项
    // - 创建快捷方式脚本
    // - 执行安装命令
    // - 如果不是 silent 模式，启动程序
}
```

---

## 📊 安装选项详解

### desktopicon - 桌面图标

**效果：** 在公共桌面创建快捷方式

**Windows 路径：**
```
%PUBLIC%\Desktop\RustDesk.lnk
```

**代码检查：**
```rust
if options.contains("desktopicon") {
    shortcuts = format!(
        "copy /Y \"{}\\{}.lnk\" \"%PUBLIC%\\Desktop\\\"",
        tmp_path, app_name
    );
}
```

### startmenu - 开始菜单

**效果：** 在开始菜单创建程序组

**Windows 路径：**
```
%PROGRAMDATA%\Microsoft\Windows\Start Menu\Programs\RustDesk\
```

**包含项目：**
- RustDesk.lnk - 主程序快捷方式
- Uninstall RustDesk.lnk - 卸载程序快捷方式

### printer - 虚拟打印机（仅Windows）

**效果：** 安装 RustDesk 虚拟打印机驱动

**用途：** 远程打印功能

**注意：** 
- ⚠️ 需要管理员权限
- ⚠️ 仅在 Windows 平台可用
- ⚠️ 可能需要重启系统

---

## 🛠️ 自定义安装选项

### 方法 1：修改默认选项（编译时）

**修改文件：** `src/core_main.rs` 第 301-303 行

```rust
// 示例：不创建打印机
#[cfg(windows)]
let options = "desktopicon startmenu";  // 移除 printer

// 示例：只创建开始菜单
#[cfg(windows)]
let options = "startmenu";

// 示例：什么都不创建
#[cfg(windows)]
let options = "";
```

### 方法 2：通过命令行参数（需要代码修改）

**扩展代码示例：**

```rust
// 在 core_main.rs 中添加自定义选项解析
} else if args[0] == "--silent-install" {
    let mut options = String::new();
    
    // 解析自定义选项
    for arg in &args[1..] {
        if arg == "desktop" {
            options.push_str("desktopicon ");
        } else if arg == "startmenu" {
            options.push_str("startmenu ");
        } else if arg == "printer" {
            options.push_str("printer ");
        }
    }
    
    // 如果没有指定选项，使用默认值
    if options.is_empty() {
        #[cfg(windows)]
        let options = "desktopicon startmenu printer";
        #[cfg(not(windows))]
        let options = "desktopicon startmenu";
    }
    
    let res = platform::install_me(&options.trim(), "".to_owned(), true, false);
    // ...
}
```

**使用：**
```powershell
# 只创建桌面图标
rustdesk.exe --silent-install desktop

# 创建桌面图标和开始菜单
rustdesk.exe --silent-install desktop startmenu

# 完整安装
rustdesk.exe --silent-install desktop startmenu printer
```

---

## 🔒 权限要求

### Windows

| 操作 | 所需权限 | 说明 |
|------|---------|------|
| 安装到 Program Files | **管理员** | 需要写入系统目录 |
| 创建桌面图标 | **管理员** | 写入公共桌面 |
| 创建开始菜单 | **管理员** | 写入系统菜单 |
| 注册服务 | **管理员** | 必须有管理员权限 |
| 安装打印机 | **管理员** | 需要安装驱动 |

### 提升权限

```powershell
# PowerShell 以管理员身份运行
Start-Process rustdesk.exe -ArgumentList "--silent-install" -Verb RunAs
```

---

## 🧪 测试静默安装

### 测试脚本

```powershell
# test_silent_install.ps1
param(
    [string]$InstallerPath = ".\rustdesk.exe"
)

Write-Host "========================================" -ForegroundColor Cyan
Write-Host "  RustDesk 静默安装测试" -ForegroundColor Cyan
Write-Host "========================================" -ForegroundColor Cyan
Write-Host ""

# 检查安装文件
if (-not (Test-Path $InstallerPath)) {
    Write-Host "错误: 找不到安装文件 $InstallerPath" -ForegroundColor Red
    exit 1
}

Write-Host "[1/4] 开始静默安装..." -ForegroundColor Yellow
& $InstallerPath --silent-install debug

Start-Sleep -Seconds 3

Write-Host "[2/4] 检查安装结果..." -ForegroundColor Yellow
$programPath = "C:\Program Files\RustDesk\rustdesk.exe"
if (Test-Path $programPath) {
    Write-Host "  ✅ 程序已安装: $programPath" -ForegroundColor Green
} else {
    Write-Host "  ❌ 程序未找到" -ForegroundColor Red
}

Write-Host "[3/4] 检查桌面快捷方式..." -ForegroundColor Yellow
$desktopShortcut = "$env:PUBLIC\Desktop\RustDesk.lnk"
if (Test-Path $desktopShortcut) {
    Write-Host "  ✅ 桌面快捷方式已创建" -ForegroundColor Green
} else {
    Write-Host "  ❌ 桌面快捷方式未找到" -ForegroundColor Red
}

Write-Host "[4/4] 检查服务..." -ForegroundColor Yellow
$service = Get-Service -Name "RustDesk" -ErrorAction SilentlyContinue
if ($service) {
    Write-Host "  ✅ 服务已安装: $($service.Status)" -ForegroundColor Green
} else {
    Write-Host "  ❌ 服务未找到" -ForegroundColor Red
}

Write-Host ""
Write-Host "测试完成！" -ForegroundColor Cyan
```

---

## 🗑️ 静默卸载

### 卸载命令

```powershell
# 静默卸载
rustdesk.exe --uninstall

# 或者通过已安装的卸载程序
"C:\Program Files\RustDesk\uninstall.exe" /S
```

### 卸载过程

```
执行卸载
    ↓
停止 RustDesk 服务
    ↓
删除文件
    ├─ Program Files\RustDesk\*
    ├─ 桌面快捷方式
    ├─ 开始菜单快捷方式
    └─ 配置文件（可选保留）
    ↓
删除注册表项
    ↓
卸载虚拟打印机（如果安装了）
    ↓
卸载完成
```

---

## 🌐 企业部署场景

### GPO 部署（Group Policy）

```powershell
# 1. 准备安装脚本
# gpo_install.ps1
$installerUrl = "https://your-server.com/rustdesk.exe"
$installerPath = "$env:TEMP\rustdesk.exe"

# 下载安装程序
Invoke-WebRequest -Uri $installerUrl -OutFile $installerPath

# 静默安装
& $installerPath --silent-install

# 清理
Remove-Item $installerPath -Force
```

### SCCM/Intune 部署

```xml
<!-- 部署配置示例 -->
<Application>
  <Name>RustDesk</Name>
  <InstallCommand>rustdesk.exe --silent-install</InstallCommand>
  <UninstallCommand>rustdesk.exe --uninstall</UninstallCommand>
  <DetectionMethod>
    <FileExists>C:\Program Files\RustDesk\rustdesk.exe</FileExists>
  </DetectionMethod>
</Application>
```

---

## 📝 注意事项

### ⚠️ 重要提示

1. **管理员权限**
   - 静默安装需要管理员权限
   - 使用 `RunAs` 或以管理员身份运行

2. **防病毒软件**
   - 某些防病毒软件可能拦截静默安装
   - 需要添加白名单或临时禁用

3. **安装位置**
   - 默认安装到 `C:\Program Files\RustDesk\`
   - 暂不支持自定义安装路径（需修改代码）

4. **通知提示**
   - 即使是静默安装，也会显示 Toast 通知
   - 提示安装成功或失败

5. **服务自动启动**
   - 安装后 RustDesk 服务会自动启动
   - 设置为开机自启动

---

## 🔍 故障排查

### 常见问题

**1. 安装失败：权限不足**

```
解决方案：以管理员身份运行
PowerShell: Start-Process rustdesk.exe -ArgumentList "--silent-install" -Verb RunAs
```

**2. 服务未启动**

```powershell
# 手动启动服务
Start-Service RustDesk

# 检查服务状态
Get-Service RustDesk
```

**3. 快捷方式未创建**

```
检查：
1. 是否包含 desktopicon/startmenu 选项
2. 是否有写入权限
3. 临时文件夹是否可访问
```

**4. 打印机安装失败（Windows）**

```
可能原因：
1. 权限不足
2. 打印服务未运行
3. 驱动签名问题

解决：移除 printer 选项
```

---

## 📚 相关文档

- **[修改说明.md](修改说明.md)** - 完整的修改说明
- **[自定义构建说明.md](自定义构建说明.md)** - 快速开始指南
- **[隐藏连接窗口说明.md](隐藏连接窗口说明.md)** - 窗口隐藏技术

---

## 🎉 总结

RustDesk 的静默安装功能非常适合企业批量部署：

✅ **简单易用** - 单个命令完成安装  
✅ **无需交互** - 完全自动化  
✅ **灵活配置** - 可自定义安装选项  
✅ **企业友好** - 支持 GPO/SCCM/Intune

**基本命令：**
```powershell
# 管理员身份运行
rustdesk.exe --silent-install
```

---

**文档版本：** 1.0  
**最后更新：** 2025-11-18  
**适用版本：** RustDesk 1.x
