# 🔧 RustDesk Web Client 版本要求

## ✅ 必需版本

### Flutter SDK

```bash
# 最低版本
Flutter: 3.24.0+
Dart: 3.5.0+

# 推荐版本
Flutter: 3.24.5
Dart: 3.5.4

# 检查当前版本
flutter --version
```

---

## 📦 主要依赖

| 包名 | 版本 | Dart SDK 要求 |
|------|------|---------------|
| `extended_text` | 14.0.0 | >= 3.5.0 < 4.0.0 |
| `flutter` | SDK 内置 | >= 3.5.0 |

---

## 🔍 版本对应关系

| Flutter 版本 | Dart SDK 版本 | 状态 |
|-------------|--------------|------|
| 3.24.5 | 3.5.4 | ✅ 推荐 |
| 3.24.0 | 3.5.0 | ✅ 可用 |
| 3.22.0 | 3.4.0 | ⚠️ 不兼容 |
| 3.19.0 | 3.3.0 | ❌ 不兼容 |

---

## 🚀 升级 Flutter

### 检查当前版本

```bash
flutter --version
```

### 升级到最新稳定版

```bash
# 切换到 stable 频道
flutter channel stable

# 升级
flutter upgrade

# 验证版本
flutter --version
```

### 升级到指定版本

```bash
# 使用 FVM (Flutter Version Management)
fvm install 3.24.5
fvm use 3.24.5

# 或者使用 git
cd /path/to/flutter
git fetch
git checkout 3.24.5
flutter doctor
```

---

## 🐛 常见问题

### 问题：Dart SDK 版本过低

```
Because flutter_hbb depends on extended_text 14.0.0 which requires SDK version >=3.5.0 <4.0.0, version solving failed.
```

**解决方案：**
```bash
# 1. 升级 Flutter
flutter upgrade

# 2. 清理缓存
flutter clean
flutter pub cache repair

# 3. 重新获取依赖
flutter pub get

# 4. 重新编译
flutter build web --release
```

---

### 问题：GitHub Actions 构建失败

**检查 workflow 配置：**
```yaml
- name: Setup Flutter
  uses: subosito/flutter-action@v2
  with:
    flutter-version: '3.24.5'  # 确保版本 >= 3.24.0
    channel: 'stable'
    cache: true
```

---

## ⚙️ 本地开发环境配置

### Windows

```cmd
# 1. 下载 Flutter 3.24.5+
# https://docs.flutter.dev/get-started/install/windows

# 2. 设置环境变量
# 添加 Flutter bin 目录到 PATH

# 3. 运行配置
flutter config --no-analytics
flutter doctor

# 4. 验证
flutter --version
```

### Linux/macOS

```bash
# 1. 克隆 Flutter 仓库
cd ~
git clone https://github.com/flutter/flutter.git -b stable

# 2. 切换到指定版本
cd flutter
git checkout 3.24.5

# 3. 添加到 PATH
export PATH="$PATH:`pwd`/flutter/bin"

# 4. 运行配置
flutter config --no-analytics
flutter doctor

# 5. 验证
flutter --version
```

---

## 📊 性能对比

| Flutter 版本 | 编译时间 | 包大小 | Dart SDK |
|-------------|---------|--------|----------|
| 3.24.5 | ~5 分钟 | ~20 MB | 3.5.4 |
| 3.22.0 | ~5 分钟 | ~18 MB | 3.4.0 |
| 3.19.0 | ~4 分钟 | ~15 MB | 3.3.0 ❌ |

---

## ✅ 验证清单

### 开发环境

- [ ] Flutter 版本 >= 3.24.0
- [ ] Dart SDK 版本 >= 3.5.0
- [ ] `flutter doctor` 无错误
- [ ] `flutter pub get` 成功

### GitHub Actions

- [ ] workflow 中 Flutter 版本设置为 3.24.5
- [ ] Actions 构建成功
- [ ] 无依赖冲突错误

### 本地编译

- [ ] `flutter build web --release` 成功
- [ ] `build/web` 目录存在
- [ ] 本地测试访问正常

---

## 🔗 相关资源

| 资源 | 链接 |
|------|------|
| Flutter 官网 | https://flutter.dev |
| Flutter 发布说明 | https://docs.flutter.dev/release/release-notes |
| Dart SDK 发布 | https://dart.dev/get-dart |
| Flutter 安装指南 | https://docs.flutter.dev/get-started/install |

---

## 📝 更新日志

### v2.2.1 (2024-12-01)

- ✅ 升级 Flutter 到 3.24.5
- ✅ 满足 extended_text 14.0.0 要求
- ✅ 修复 GitHub Actions 构建问题
- ✅ 更新文档版本要求

---

**最后更新：** 2024-12-01  
**维护者：** xiehan12
